const source=document.getElementById('source'),list=document.getElementById('saved-pets'),button=document.getElementById('export'),status=document.getElementById('status');
function refresh(){try{const b=HouseSaves.bundle(source.value);list.replaceChildren();for(const [id,s] of Object.entries(b.profiles)){const p=document.createElement('div');p.textContent=id+' — '+s.pet.name+' · '+s.coins+' coins';list.append(p);}button.disabled=!Object.keys(b.profiles).length;}catch(e){status.textContent=e.message;button.disabled=true;}}
source.addEventListener('change',refresh);button.addEventListener('click',()=>{const b=HouseSaves.bundle(source.value),url=URL.createObjectURL(new Blob([JSON.stringify(b,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download='craepets-family-'+source.value+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);status.textContent='Family backup downloaded. Import it using Family → Load existing saves in the house edition.';});refresh();
